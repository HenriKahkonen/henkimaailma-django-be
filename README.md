# henkimaailma-django-be
A Python Django backend for my personal homepage Henkimaailma

## Installation

For local development, activate venv and install dependencies:
```
#Linux/MacOS

python -m venv
source venv/bin/activate
pip install -r requirements.txt
```
